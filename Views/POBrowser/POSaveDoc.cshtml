
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>POSaveDoc</title>
</head>
<body>
   保存结果： @Html.Raw(ViewBag.saveResult)
    <!--要输出保存页面的内容，就必须要用@Html.Raw(ViewBag.POShowPage)-->
    @Html.Raw(ViewBag.POShowPage)
</body>
</html>
